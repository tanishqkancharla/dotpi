## Tool Preferences

- Don't use `read_web_page` for GitHub or Notion pages; use `gh` or the `ntn` CLI, respectively, instead.
- If `read_web_page` doesn't work, use `npx libretto` to access the page through a browser instead.
